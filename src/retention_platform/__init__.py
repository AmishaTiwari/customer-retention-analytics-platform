"""Customer Retention Analytics Platform — installable ML package.

Implements the staged pipeline defined in the locked ML System Design v1.0:
raw data -> SQL preparation -> modeling dataset -> features -> training ->
evaluation -> inference.
"""
